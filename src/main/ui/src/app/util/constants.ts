
/**
 * Paths for the api calls.
 */
const PATHS = {
  LOGIN: '/api/account/login',
  LOGOUT: '/api/logout',
  REGISTER: '/api/account/register'
};

const ROLES = {
  EMPLOYEE: 'employee',
  MANAGER: 'manager'
};

// Export constants
export let constants = {
  PATHS,
  ROLES
};
